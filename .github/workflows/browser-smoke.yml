name: PhotoMosaic Browser Smoke
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: photomosaic-browser-smoke-${{ github.ref }}
  cancel-in-progress: true
jobs:
  smoke:
    if: ${{ github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install browser test runner
        run: |
          npm install --no-save @playwright/test
          npx playwright install --with-deps chromium
      - name: Start app
        run: python3 -m http.server 4173 >/tmp/photomosaic-http.log 2>&1 &
      - name: Run split-flow, mobile, wall-project, accessibility and user-behavior gates
        id: gate
        shell: bash
        run: |
          set +e
          npx playwright test qa/browser-smoke.spec.js qa/split-flows.spec.js qa/mobile-layout.spec.js qa/wall-project-v30.spec.js qa/accessibility.spec.js qa/user-100-sim.spec.js --reporter=line > /tmp/photomosaic-gate.log 2>&1
          code=$?
          set -e
          mkdir -p qa
          {
            echo 'PHOTOMOSAIC FAST RELEASE GATE'
            echo "SOURCE_COMMIT: $GITHUB_SHA"
            if [ "$code" -eq 0 ]; then echo 'RESULT: PASS'; else echo 'RESULT: FAIL'; fi
            echo "EXIT_CODE: $code"
            echo "UTC: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            echo 'COVERAGE: browser + split flows + 320/375/390/430 mobile + wall project + accessibility + 28 user-behavior scenarios'
            echo '--- TEST OUTPUT (tail) ---'
            tail -n 420 /tmp/photomosaic-gate.log
          } > qa/latest-release-gate.txt
          echo "code=$code" >> "$GITHUB_OUTPUT"
      - name: Persist gate evidence
        if: ${{ always() && github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'PhotoMosaic QA Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add qa/latest-release-gate.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'QA result: PhotoMosaic fast release gate [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Enforce gate
        if: always()
        run: test "${{ steps.gate.outputs.code }}" = "0"
