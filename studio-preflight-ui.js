(()=>{'use strict';const $=id=>document.getElementById(id);const tileMeta=new Map();let targetMeta=null;const key=f=>`${f.name}|${f.size}|${f.lastModified}`;async function meta(file){try{if('createImageBitmap'in globalThis){const b=await createImageBitmap(file);const out={name:file.name,size:file.size,width:b.width,height:b.height,fingerprint:key(file)};b.close?.();return out}return await new Promise((res,rej)=>{const u=URL.createObjectURL(file),i=new Image;i.onload=()=>{URL.revokeObjectURL(u);res({name:file.name,size:file.size,width:i.naturalWidth,height:i.naturalHeight,fingerprint:key(file)})};i.onerror=()=>{URL.revokeObjectURL(u);rej(Error('No se pudo leer '+file.name))};i.src=u})}catch{return{name:file.name,size:file.size,width:0,height:0,fingerprint:key(file)}}}
function ensure(){if($('preflightSection'))return;const section=document.createElement('section');section.id='preflightSection';section.innerHTML='<h2><b>✓</b> Control de calidad</h2><div id="preflightCard" class="shape-info">Selecciona fotos para analizar la calidad del proyecto.</div>';($('tilesSection')||$('settingsSection'))?.after(section)}
function mode(){return document.documentElement.dataset.photomosaicMode==='shape'?'shape':'mosaic'}function render(){ensure();const engine=globalThis.PhotoMosaicPreflight;if(!engine)return;const r=engine.assessProject({mode:mode(),tiles:[...tileMeta.values()],target:targetMeta,photoWidthCm:+($('photoWidth')?.value||10),photoHeightCm:+($('photoHeight')?.value||15)}),labels={blocked:'Bloqueado',excellent:'Excelente',good:'Correcto',review:'Revisar'},card=$('preflightCard');if(!card)return;card.innerHTML=`<strong>Preparación ${r.score}/100 · ${labels[r.status]}</strong><p>${r.count} fotos analizadas${r.printRisk?` · ${r.printRisk} con riesgo de impresión`:''}${r.duplicates?` · ${r.duplicates} grupos duplicados`:''}</p>${r.blockers.map(x=>`<p>⛔ ${x}</p>`).join('')}${r.warnings.map(x=>`<p>⚠️ ${x}</p>`).join('')}${r.notes.map(x=>`<p>✅ ${x}</p>`).join('')}`;}
function bind(){ensure();const tiles=$('tiles'),target=$('target'),clear=$('clear');tiles?.addEventListener('change',async e=>{const files=[...(e.target.files||[])];for(const f of files)tileMeta.set(key(f),await meta(f));render()},{capture:true});target?.addEventListener('change',async e=>{const f=e.target.files?.[0];if(f)targetMeta=await meta(f);render()},{capture:true});clear?.addEventListener('click',()=>{tileMeta.clear();render()});['photoWidth','photoHeight','photoGap'].forEach(id=>$(id)?.addEventListener('input',render));render()}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',bind,{once:true}):bind();})();