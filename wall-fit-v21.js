(function(root,factory){'use strict';const api=factory();if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.PhotoMosaicWallFit=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';
const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
function assess(plan,wall={}){if(!plan||!Number.isFinite(+plan.widthCm)||!Number.isFinite(+plan.heightCm))throw new Error('Plano no válido');const width=finite(wall.widthCm),height=finite(wall.heightCm),margin=Math.max(0,finite(wall.marginCm,10));if(width<=0||height<=0)throw new Error('Medidas de pared no válidas');const usableW=Math.max(0,width-margin*2),usableH=Math.max(0,height-margin*2),fits=plan.widthCm<=usableW&&plan.heightCm<=usableH,scale=Math.min(usableW/plan.widthCm,usableH/plan.heightCm),left=Math.max(0,(width-plan.widthCm)/2),top=Math.max(0,(height-plan.heightCm)/2);return{fits,wallWidthCm:width,wallHeightCm:height,usableWidthCm:usableW,usableHeightCm:usableH,designWidthCm:plan.widthCm,designHeightCm:plan.heightCm,leftCm:left,topCm:top,scaleToFit:Number(scale.toFixed(3)),overflowWidthCm:Math.max(0,plan.widthCm-usableW),overflowHeightCm:Math.max(0,plan.heightCm-usableH)};}
return{assess};});
